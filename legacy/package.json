{
  "name": "snowgloo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format-all": "prettier --write 'web-{server,client}/src/**/*.{js,html,css}'",
    "develop": "concurrently 'cd web-server && nodemon ./src/index.js --ignore ./src/web-build' 'cd web-client && npm start'",
    "test": "cd web-server && npm run test"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^6.3.0",
    "nodemon": "^2.0.13",
    "prettier": "^2.4.1"
  },
  "dependencies": {}
}
